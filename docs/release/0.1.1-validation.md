# stablr 0.1.1 validation record

Date: 2026-07-22

## Local environment

- Runtime: `R4_51` conda environment, R 4.5.1 on Rocky Linux 8.10.
- Compiler shown by `R CMD check`: conda GCC/G++ 14.2.0 for package build.
- Source tree: local `stabl_lw` worktree; no immutable release SHA has been
  frozen yet.

## Commands and results

- `devtools::document()`: completed after the integrated OOF, MVR, parity,
  methodology, and documentation changes. Roxygen preserved the intentionally
  hand-maintained cooperative S3 registrations.
- Full source-tree suite: `FAIL 0 | WARN 0 | SKIP 0 | PASS 1945`.
- Focused OOF late-fusion and multi-omic suites: 223 expectations passed,
  including exact named input-order invariance and sequential/parallel
  determinism.
- Focused methodology runner suite: 85 expectations passed.
- Focused pinned Python-parity suite: 71 expectations passed; manifest hashes
  and byte-identical regeneration also passed.
- Focused MVR solver/provenance suites: 40 expectations passed.
- Methodology CLI smoke profile: completed with one null replicate, two
  bootstraps, and two lambdas, writing schema, parity, warning, manifest, and
  gate artifacts to `/tmp/stablr-methodology-smoke`.

## Methodology status and stop condition

The locked `release` profile is defined as 100 paired replicates, 1,000
bootstraps, and 30 lambdas, with explicit one-sided null and signal gates. It
has not yet been completed. The release must stop unless every gate passes.
The current fitting rule chooses the minimum of the observed FDP+ curve;
`fdr_target` is a plotting reference only, not a fitting target or universal
FDP-control guarantee.

Preliminary Slurm job `25315926` was canceled on 2026-07-22 because it had
started from a mutable worktree. Its outputs are not release evidence. The
final locked matrix will be launched only from a clean candidate commit; its
manifest records the commit, Git tree, tracked dirty state, and SHA-256 hashes
of every result artifact. Raw artifacts are written to
`/exports/para-lipg-hpc/mdmanurung/stablr-validation-artifacts/0.1.1/methodology/results-source`,
outside the repository. The earlier bounded evidence failed the release-style
gates and is not used as positive evidence; no defaults were tuned against it.

## Late-fusion behavior

The default now trains stacking weights from one OOF prediction per training
sample. STABL selection and downstream fitting occur wholly inside each fold;
groups remain assessment-disjoint. Full-training selectors and downstream
models are refitted only after weights are fixed. Expected fitting cost for
five-fold OOF plus the full refit is approximately six times the historical
single-fit path. On the fixed benchmark workload (60 samples, two 10-feature
omics, 10 bootstraps, three repetitions), median elapsed time was 0.378 s for
OOF (IQR 0.377--0.448) and 0.057 s for legacy (IQR 0.057--0.174), a 6.63x
ratio. Base-R GC maximum memory was about 198 MB versus 147 MB. The repetition
count is intentionally small and does not support a general speed claim.
`Rprof`, `Rprofmem`, call-stack summaries, allocation evidence, and the exact
manifest are stored under
`/exports/para-lipg-hpc/mdmanurung/stablr-validation-artifacts/0.1.1/performance`.
`late_fusion_training = "python_legacy"` preserves the historical in-sample
weighting and RNG behavior.

The first required 50-replicate paired OOF-versus-legacy matrix, Slurm job
`25315916`, failed its predeclared completeness gate: five of six cells
completed, while multinomial signal completed only 47/50 replicates because
rare classes made three selector fits ill posed. Its evidence is retained under
`/exports/para-lipg-hpc/mdmanurung/stablr-validation-artifacts/0.1.1/late-fusion/results`
and is not positive release evidence.

Before any rerun, the feasibility protocol was amended and recorded here:
classification simulations require at least ten observations per class in
both training and independent-test partitions; stacking folds and bootstrap
samples are outcome-stratified; per-omic selector failures inside a stacking
fold use that fold's training class prior or mean with explicit provenance;
and optimism is the directional train-minus-test difference rather than its
absolute value. These changes correct an ill-posed simulation and implement
the predeclared fallback contract; they do not change the noninferiority,
fallback-rate, or optimism gates and were not chosen to make an observed gate
pass. The replacement run will start only from the immutable clean candidate.
Independent test samples never enter selectors, downstream learners, or
stacking weights.

## Outstanding external evidence

Current-R-devel, Windows, macOS, Win-builder, R-hub sanitizer, and GitHub CI
results are not yet available. The Python reference is pinned to Python 3.11.9
and `gregbellan/Stabl@1d07f85a13cfbecb4f08ce21075bf4fbb8e34678`; complete
frozen inputs, outputs, solver-ranking contracts, generator, provenance, and
hash manifest are committed. No CRAN or GitHub release action is authorized by
this local record until every statistical and platform gate passes.
