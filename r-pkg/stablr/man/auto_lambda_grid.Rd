% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learner_adapters.R
\name{auto_lambda_grid}
\alias{auto_lambda_grid}
\title{Build a Data-Driven Lambda Grid}
\usage{
auto_lambda_grid(x, y, family = "gaussian", n_lambda = 30L, l1_ratio = NULL)
}
\arguments{
\item{x}{Numeric matrix of predictors (samples by features).}

\item{y}{Outcome vector (numeric for \code{"gaussian"}, factor or 0/1 integer
for \code{"binomial"} or \code{"multinomial"}).}

\item{family}{Character; \code{glmnet} response family.}

\item{n_lambda}{Integer; desired number of lambda values.}

\item{l1_ratio}{Numeric scalar, numeric vector, or \code{NULL}.}
}
\value{
A \code{data.frame} with at least a \code{lambda} column; an \code{alpha} column
is included whenever \code{l1_ratio} is not \code{NULL}.
}
\description{
Runs \code{glmnet} on the full data to obtain a calibrated penalty sequence,
returning the result as a pre-expanded \code{data.frame} suitable for use as
the \code{lambda_grid} argument of \code{\link[=stabl_fit]{stabl_fit()}}.
}
\details{
This mirrors \code{auto_mode_lambda_grid()} in \code{stabl/utils.py}, but delegates
sequence computation to \code{glmnet}'s own path algorithm.

For elastic-net models, a vector of \code{l1_ratio} values may be supplied; the
function fits a separate path for each \code{alpha} value and row-binds the
results, adding an \code{alpha} column so that \code{\link[=make_glmnet_adapter]{make_glmnet_adapter()}} can read
it.
}
