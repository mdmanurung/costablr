% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_stabl_path}
\alias{plot_stabl_path}
\title{Plot the STABL Stability Path}
\usage{
plot_stabl_path(
  object,
  new_hard_threshold = NULL,
  title = "STABL Stability Path"
)
}
\arguments{
\item{object}{A fitted \code{"stabl_fit"} object from \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{new_hard_threshold}{Numeric in \verb{(0, 1]} or \code{NULL}.  When supplied,
overrides the threshold stored in \code{object}.}

\item{title}{Character; plot title (default \code{"STABL Stability Path"}).}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a ggplot2 line chart showing the stability score (frequency of
selection) of each feature across the regularisation path.  Selected
features are highlighted in red; noise features are shown in dark grey.
When artificial features were used during fitting, their stability path is
overlaid as thin dotted grey lines.  The FDP+-optimal (or hard) threshold
is shown as a dashed horizontal line.
}
\details{
When the lambda grid contains an \code{alpha} column (elastic-net mixed-alpha
path), the plot is automatically faceted by \code{alpha}.
}
