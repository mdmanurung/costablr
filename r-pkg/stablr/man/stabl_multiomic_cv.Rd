% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiomic_workflows.R
\name{stabl_multiomic_cv}
\alias{stabl_multiomic_cv}
\title{Minimal Multi-Omic STABL Cross-Validation Workflow}
\usage{
stabl_multiomic_cv(
  x_list,
  y,
  lambda_grid,
  v = 5L,
  groups = NULL,
  base_learner = "lasso",
  family = "gaussian",
  n_bootstraps = 100L,
  artificial_type = "random_permutation",
  hard_threshold = NULL,
  random_state = NULL,
  ...
)
}
\arguments{
\item{x_list}{Named list of omic tables (\code{data.frame} or numeric matrix),
each with row names as sample IDs.}

\item{y}{Named outcome vector.}

\item{lambda_grid}{Either a shared lambda grid (\code{data.frame} or \code{"auto"})
used for all omics, or a named list mapping each omic name to its own
lambda grid.}

\item{v}{Number of folds. Must be at least 2.}

\item{groups}{Optional named grouping vector. When supplied, all samples in
the same group are assigned to the same assessment fold.}

\item{base_learner}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{family}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{n_bootstraps}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{artificial_type}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{hard_threshold}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{random_state}{Optional integer seed used for deterministic fold
assignment and forwarded to each per-fold \code{\link[=stabl_fit]{stabl_fit()}} call.}

\item{...}{Additional arguments forwarded to \code{\link[=stabl_fit]{stabl_fit()}}.}
}
\value{
A list with class \code{"stabl_multiomic_cv"} containing:
\describe{
\item{\code{folds}}{List of fold descriptors with \code{fold}, \code{train_ids}, and
\code{valid_ids}.}
\item{\code{fold_results}}{Named list of per-fold \code{stabl_multiomic_fit}
results.}
\item{\code{diagnostics}}{Data frame with one row per fold/omic and columns
\code{fold}, \code{omic}, \code{n_selected}, \code{threshold}, and \code{max_score}.}
}
}
\description{
Builds deterministic fold splits over a named multi-omic input, fits
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}} on each training fold, and returns
fold-wise selection diagnostics together with selected train/validation
matrices for downstream inspection.
}
\details{
This function keeps the first CV slice narrow: it performs per-omic STABL
feature selection only and does not add downstream predictive refits,
early fusion, or late fusion.
}