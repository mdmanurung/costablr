% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiomic_workflows.R
\name{stabl_multiomic_train_validate}
\alias{stabl_multiomic_train_validate}
\title{Minimal Multi-Omic STABL Train/Validation Workflow}
\usage{
stabl_multiomic_train_validate(
  x_train_list,
  y_train,
  lambda_grid,
  x_valid_list = NULL,
  y_valid = NULL,
  groups_train = NULL,
  base_learner = "lasso",
  family = "gaussian",
  n_bootstraps = 100L,
  artificial_type = "random_permutation",
  hard_threshold = NULL,
  random_state = NULL,
  ...
)
}
\arguments{
\item{x_train_list}{Named list of training omic tables (\code{data.frame} or
numeric matrix), each with row names as sample IDs.}

\item{y_train}{Named outcome vector for training samples.}

\item{lambda_grid}{Either a shared lambda grid (\code{data.frame} or \code{"auto"})
used for all omics, or a named list mapping each omic name to its own
lambda grid.}

\item{x_valid_list}{Optional named list of validation omic tables. When
supplied, names must match \code{x_train_list} names.}

\item{y_valid}{Optional named outcome vector for validation samples. When
supplied together with \code{x_valid_list}, sample alignment is validated.}

\item{groups_train}{Optional named grouping vector for training samples.
When supplied, grouped bootstrap sampling is used in each per-omic fit.}

\item{base_learner}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{family}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{n_bootstraps}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{artificial_type}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{hard_threshold}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{random_state}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=stabl_fit]{stabl_fit()}}.}
}
\value{
A named list with class \code{"stabl_multiomic_fit"} containing:
\describe{
\item{\code{fits}}{Named list of per-omic \code{stabl_fit} objects.}
\item{\code{selected_features}}{Named list of selected feature names.}
\item{\code{selected_train}}{Named list of training matrices restricted to
selected features (possibly 0-column).}
\item{\code{selected_valid}}{Named list of validation matrices restricted to
selected features, or \code{NULL} when no validation input is provided.}
}
}
\description{
Fits \code{\link[=stabl_fit]{stabl_fit()}} independently on each omic block from a named list,
then returns per-omic fitted objects and selected-feature matrices for
downstream composition.
}
\details{
This function intentionally keeps scope narrow for the first Phase 5
workflow slice: it orchestrates strict alignment checks and leakage-safe
grouped fitting, but does not perform downstream predictive refitting.
}
