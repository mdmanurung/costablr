% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stabl_accessors.R
\name{get_support}
\alias{get_support}
\title{Get the Feature Selection Mask from a Fitted STABL Object}
\usage{
get_support(object, new_hard_threshold = NULL)
}
\arguments{
\item{object}{A fitted \code{"stabl_fit"} object returned by \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{new_hard_threshold}{Numeric in \verb{(0, 1]} or \code{NULL}.  When supplied,
overrides the threshold stored in \code{object} for this call only.}
}
\value{
Named logical vector of length \code{object$n_features_in_}.  Names are
the original feature names from the training matrix.
}
\description{
Returns a named logical vector that is \code{TRUE} for every feature whose
maximum stability score is greater than or equal to the effective threshold,
matching the StablSRM paper-method implementation. This intentionally
differs from the upstream Python implementation's strict \code{>} tie behavior.
This is the primary accessor for downstream use of a fitted STABL model:
index your data matrix with the returned mask, or pass the object to
\code{\link[=get_feature_names_out]{get_feature_names_out()}} to obtain the names directly.
}
\details{
\strong{Threshold resolution order:}
\enumerate{
\item \code{new_hard_threshold} (if supplied to this call).
\item \code{object$hard_threshold} (if a hard threshold was given to \code{\link[=stabl_fit]{stabl_fit()}}).
\item \code{object$fdr_min_threshold_} (the FDP+-optimal threshold computed from
artificial features during fitting).
}

An error is raised when none of these is available — which can only happen
if \code{stabl_fit()} was called with both \code{artificial_type = NULL} and no
\code{hard_threshold}.

\strong{Explore fallback:} When \code{explore = TRUE} was set during fitting and no
feature's score meets the threshold, the function lowers the cutoff to the
\code{n_explore}-th largest score minus \code{0.01}, then reapplies the paper-method
\code{>=} selection rule. This can select more than \code{n_explore} features when
scores are tied.
}
\seealso{
\code{\link[=get_feature_names_out]{get_feature_names_out()}} to get names directly,
\code{\link[=get_importances]{get_importances()}} to inspect raw stability scores.
}
