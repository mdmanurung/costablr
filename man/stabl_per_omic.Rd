% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiomic_workflows.R
\name{stabl_per_omic}
\alias{stabl_per_omic}
\title{Fit STABL Separately Per Omic View}
\usage{
stabl_per_omic(
  x_train_list,
  y_train,
  lambda_grid,
  x_valid_list = NULL,
  y_valid = NULL,
  groups_train = NULL,
  base_learner = "lasso",
  family = "gaussian",
  n_bootstraps = 100L,
  artificial_type = "random_permutation",
  hard_threshold = NULL,
  stratify_bootstrap = FALSE,
  bootstrap_strata_train = NULL,
  l1_ratio = NULL,
  random_state = NULL,
  ...
)
}
\arguments{
\item{x_train_list}{Named list of training omic tables (\code{data.frame} or
numeric matrix), each with row names as sample IDs.}

\item{y_train}{Named outcome vector for training samples.}

\item{lambda_grid}{Either a shared lambda grid (\code{data.frame} or \code{"auto"})
used for all omics, or a named list mapping each omic name to its own
lambda grid. When \code{early_fusion = TRUE}, this must be a shared grid
(\code{data.frame} or \code{"auto"}); per-omic lambda lists are rejected because
Early Fusion has one concatenated input space.}

\item{x_valid_list}{Optional named list of validation omic tables. When
supplied, names must match \code{x_train_list} names.}

\item{y_valid}{Optional named outcome vector for validation samples. When
supplied together with \code{x_valid_list}, sample alignment is validated.}

\item{groups_train}{Optional named grouping vector for training samples.
When supplied, grouped bootstrap sampling is used in each per-omic fit.}

\item{base_learner}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{family}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{n_bootstraps}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{artificial_type}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{hard_threshold}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{stratify_bootstrap}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}. When \code{TRUE}, per-omic
and early-fusion STABL fits draw class-stratified bootstrap subsamples.}

\item{bootstrap_strata_train}{Optional categorical bootstrap stratification
design for training samples, forwarded to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{l1_ratio}{Passed to \code{\link[=stabl_fit]{stabl_fit()}} when \code{lambda_grid = "auto"}.
Use this with \code{base_learner = "elastic_net"} to generate alpha-aware auto
grids.}

\item{random_state}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=stabl_fit]{stabl_fit()}}.}
}
\value{
A list with class \code{"stabl_per_omic"} containing per-omic \code{fits},
per-omic final \code{refits}, selected feature names and matrices, aligned
outcomes, sample ids, family/task metadata, and optional validation
selected matrices.
}
\description{
Fits \code{\link[=stabl_fit]{stabl_fit()}} independently on each Omic View and returns a reusable
per-omic selection artifact. This object is the intended input boundary for
STABL-selected downstream fusion methods: \code{\link[=stabl_late_fusion]{stabl_late_fusion()}},
\code{\link[=stabl_multiomics]{stabl_multiomics()}}, and \code{\link[=stabl_cooperative]{stabl_cooperative()}}.
}
\details{
\code{stabl_per_omic()} does not estimate out-of-sample performance by itself.
For cross-validation, create a fresh \code{stabl_per_omic()} object inside each
training fold so that validation samples do not influence feature selection.
}
