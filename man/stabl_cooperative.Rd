% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiomic_workflows.R
\name{stabl_cooperative}
\alias{stabl_cooperative}
\title{Cooperative STABL from a Per-Omic STABL Artifact}
\usage{
stabl_cooperative(
  per_omic,
  rho = NULL,
  cooperation_selection = c("cv", "validation"),
  cooperation_selector = c("lambda.min", "lambda.1se"),
  cooperation_type_measure = "default",
  cooperation_nfolds = 5L,
  random_state = NULL
)
}
\arguments{
\item{per_omic}{A \code{"stabl_per_omic"} object returned by \code{\link[=stabl_per_omic]{stabl_per_omic()}}.}

\item{rho}{Numeric scalar or vector of non-negative cooperation strengths.}

\item{cooperation_selection}{Character scalar, \code{"cv"} or \code{"validation"}.}

\item{cooperation_selector}{Character scalar, \code{"lambda.min"} or
\code{"lambda.1se"}.}

\item{cooperation_type_measure}{Cooperative tuning metric. \code{"default"} uses
the package default for the active family.}

\item{cooperation_nfolds}{Number of inner folds when
\code{cooperation_selection = "cv"}.}

\item{random_state}{Optional integer seed. Defaults to the seed stored in
\code{per_omic}.}
}
\value{
A cooperative-fusion result list, with selected features expanded
back to all Omic Views from \code{per_omic}.
}
\description{
Fits cooperative fusion after per-omic STABL selection. The cooperative model
receives only the selected biomarkers in each Omic View, preserving STABL's
view-specific reliability-threshold boundary while adding an agreement
penalty in the final predictive layer.
}
