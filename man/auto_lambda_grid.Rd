% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learner_adapters.R
\name{auto_lambda_grid}
\alias{auto_lambda_grid}
\title{Build a Data-Driven Lambda Grid}
\usage{
auto_lambda_grid(x, y, family = "gaussian", n_lambda = 30L, l1_ratio = NULL)
}
\arguments{
\item{x}{Numeric matrix of predictors (samples by features).}

\item{y}{Outcome vector.  For \code{"gaussian"} provide a numeric vector; for
\code{"binomial"}/\code{"multinomial"} provide a factor or 0/1 integer vector;
for \code{"cox"} provide a \code{survival::Surv} object.}

\item{family}{Character; \code{glmnet} response family (\code{"gaussian"},
\code{"binomial"}, \code{"multinomial"}, or \code{"cox"}).  Default \code{"gaussian"}.}

\item{n_lambda}{Positive integer; desired number of lambda values per alpha
level.  Actual length may be slightly shorter if \code{glmnet} detects
saturation early.  Default \code{30L}.}

\item{l1_ratio}{Numeric scalar, numeric vector, or \code{NULL}.  When \code{NULL}
(default), a pure lasso path (alpha = 1) is used.  When a vector is
supplied (e.g. \code{c(0.5, 0.75, 1.0)}), one path is fitted per value and
the results are combined; an \code{alpha} column is added to the output.}
}
\value{
A \code{data.frame} with at least a \code{lambda} column.  An \code{alpha} column
is included whenever \code{l1_ratio} is not \code{NULL}.
}
\description{
Returns a Python STABL-compatible penalty sequence as a pre-expanded
\code{data.frame} suitable for use as the \code{lambda_grid} argument of
\code{\link[=stabl_fit]{stabl_fit()}}.  Gaussian, binomial, and multinomial families use the
upstream Python auto-mode formulas translated to glmnet's \code{lambda} scale.
Cox remains on glmnet's native path because upstream Python STABL has no Cox
backend.
}
\details{
For Gaussian outcomes, the path follows Python's
\verb{||X'Y||_inf / (n * l1_ratio)} scale and returns
\code{geomspace(lambda_max / 30, lambda_max + 5, n_lambda)}.  For classification,
R approximates Python's \code{sklearn.svm.l1_min_c()} grid, converts the resulting
\code{C} path to glmnet-style \code{lambda = 1 / C}, and returns that decreasing
penalty sequence.

For elastic-net models, supplying a vector of \code{l1_ratio} values causes the
function to fit a separate path per alpha, row-bind the resulting grids,
and add an \code{alpha} column so that \code{\link[=make_glmnet_adapter]{make_glmnet_adapter()}} can dispatch
correctly.  This mirrors \code{auto_mode_lambda_grid()} in the Python reference
implementation.
}
\seealso{
\code{\link[=stabl_fit]{stabl_fit()}} which calls this automatically when
\code{lambda_grid = "auto"}.
}
